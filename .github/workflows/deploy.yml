# 5. Teste de Diagnóstico de Rede e SSH
    - name: SSH Debug Test
      run: |
        echo "1. A testar se o VPS responde na porta web 80:"
        nc -zv -w 5 ${{ secrets.VPS_IP }} 80
        echo "2. A testar se o VPS responde na porta SSH 22:"
        nc -zv -w 5 ${{ secrets.VPS_IP }} 22